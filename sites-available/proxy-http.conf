server {
  listen   80; ## listen for ipv4
  listen   [::]:80 default ipv6only=on; ## listen for ipv6
  server_name  _;
  access_log  /var/log/nginx/http.access.log;
  location / { proxy_pass http://groupsite/; }
}

# vim:ft=nginx
